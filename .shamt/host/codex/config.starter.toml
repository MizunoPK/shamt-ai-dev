# .codex/config.toml — Shamt-managed Codex project config
# Written by init.sh (Shamt SHAMT-42). User-editable outside managed blocks.
# Run regen-codex-shims.sh to refresh managed sections.

[model]
# Set by regen-codex-shims.sh from .shamt/host/codex/.model_resolution.local.toml
# Override here or use --profile at session start.

[agents]
max_depth = 1  # Codex ceiling; Shamt validation loops flatten to depth-1

# =============================================================================
# SHAMT-PROFILES-START — managed by regen-codex-shims.sh; do not edit manually
# =============================================================================

# (profile fragments inserted here by regen-codex-shims.sh)

# =============================================================================
# SHAMT-PROFILES-END
# =============================================================================

# =============================================================================
# SHAMT-MCP-START — managed by regen-codex-shims.sh; do not edit manually
# =============================================================================

# (MCP server registration inserted here by regen-codex-shims.sh when venv present)

# ADO MCP Server — inserted by regen-codex-shims.sh when pr_provider.conf contains "ado"
# [mcp_servers.ado]
# command = "npx"
# args = ["-y", "@azure-devops/mcp", "{ado_org}", "-d", "core", "repositories"]
# type = "stdio"

# =============================================================================
# SHAMT-MCP-END
# =============================================================================

# =============================================================================
# SHAMT-HOOKS-START — managed by regen-codex-shims.sh; do not edit manually
# =============================================================================

# (hook registrations inserted here by regen-codex-shims.sh)

# =============================================================================
# SHAMT-HOOKS-END
# =============================================================================

# =============================================================================
# OTel / Tracing (optional — uncomment to enable OpenTelemetry)
# See .shamt/observability/README.md for collector setup and dashboard import.
# =============================================================================
# [telemetry]
# otlp_endpoint = "http://localhost:4317"   # local Docker collector (default)
# # otlp_endpoint = "http://otel-collector:4317"  # shared/org collector
# # otlp_transport = "grpc"                        # grpc (default) or http
