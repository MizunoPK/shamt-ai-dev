# Shamt Import Workflow

This guide describes what the agent does after the user runs `import.sh` (or `import.ps1`). The import script handles the file sync mechanically; this guide covers the validation work required afterward.

The import script prints a ready-to-use agent prompt at the end of its run. This guide is the full reference behind that prompt.

---

## When This Applies

You are reading this guide because the user has just run:

```bash
bash .shamt/scripts/import/import.sh
# or on Windows:
# & ".shamt\scripts\import\import.ps1"
```

The script has already:
- Copied updated/new files from master's `guides/` and `scripts/` into your `.shamt/`
- Removed files that were deleted from master
- Generated one or more diff files at `.shamt/import_diff.md` (or `.shamt/import_diff_1.md`, `import_diff_2.md`, etc. if the diff was large)

Your job is to assess the impact and ensure your project-specific configuration stays consistent.

---

## Mandatory First Step: Re-Read the Validation Loop Protocol

**Before doing anything else**, re-read:

`.shamt/guides/reference/validation_loop_master_protocol.md`

You will run a validation loop (3 consecutive clean rounds required) after completing the assessment below.

---

## Step 1: Read the Import Diff

Read the diff file(s) generated by the import script:

- If `.shamt/import_diff.md` exists, read that file
- If `.shamt/import_diff_1.md`, `import_diff_2.md`, etc. exist, read all of them in order

The diffs show exactly what changed in each shared file (child content → master content). Read them carefully before making any decisions.

---

## Step 2: For Each Changed File, Check Supplements

For each file shown in the diff, ask:

1. Does this project have a supplement in `.shamt/project-specific-configs/guides/[mirrored-path]/`?
2. If yes — does the supplement still make sense given the new guide content?
3. Is the supplement still accurate, or did the master update change the context it was written for?

Update any supplements that are now inaccurate or inconsistent with the new guide content.

---

## Step 3: Check Pointers

For each changed file:

1. Check whether any pointer notes in the updated guide file are still accurately placed
2. If a section was moved, renamed, or removed — the pointer may now reference the wrong location
3. Update or remove stale pointers

Also check whether the new guide content has introduced new extension points that would benefit from a pointer + supplement for this project. If so, create the supplement and add the pointer.

---

## Step 4: Check for New Files and Deletions

The import summary (printed by the script) lists any newly added or deleted files.

For new files from master:
- Read them to understand what they add to the guide system
- Assess whether this project needs a corresponding `project-specific-configs/` supplement

For deleted files:
- Confirm the deletion makes sense (the guide was retired or merged elsewhere)
- Check if any of your `project-specific-configs/` supplements referenced the deleted guide — if so, update or remove them

---

## Step 5: Run a Validation Loop

Run a full validation loop using the master protocol (which you re-read in the mandatory first step). The loop must achieve 3 consecutive clean rounds before you may declare the import complete.

Check for:
- All supplements still accurate and consistent with updated guides
- All pointers correctly placed and not stale
- No project-specific content inadvertently introduced into shared guides
- No broken cross-references in updated guides (if the import introduced any)
- `SHAMT-{N}` placeholder intact throughout `.shamt/guides/` — you must not replace it with your epic tag

**On the last point:** `SHAMT-{N}` appears throughout the shared guides as a generic placeholder for "epic number N." It is not a reference to the master Shamt project — it applies equally to every child project. Never substitute it with your project's epic tag (e.g., do not change `SHAMT-{N}` to `KAI-{N}` or `BAR-{N}`). If you find any such substitutions, revert them immediately before the validation loop can pass.

---

## Step 6: Delete the Diff Files

When the validation loop passes 3 clean rounds, delete all import diff files:

```bash
rm -f .shamt/import_diff.md .shamt/import_diff_*.md
```

```powershell
# PowerShell:
Remove-Item .shamt\import_diff.md, .shamt\import_diff_*.md -ErrorAction SilentlyContinue
```

Do not leave diff files in place after the import is complete — they are temporary and contain dated content.

---

## Step 7: Commit

Stage and commit the results of the import:

```bash
git add .shamt/guides/ .shamt/scripts/ .shamt/project-specific-configs/
git commit -m "sync: Import Shamt updates from master (YYYY-MM-DD)

[Brief description of what changed]"
```

Include only files that were part of the import or supplement updates. Do not bundle other work into this commit.

---

## Common Situations

**No diff files generated, only new files or deletions:**
- Skim the new files to understand them
- Check whether any deletions affect your supplements
- Validation loop is still required, but typically shorter

**Large import with many changed files:**
- Work through one file at a time; don't try to assess all at once
- The diff was split into numbered files precisely so each is readable in one pass
- Prioritize guides you use frequently (S5, S7, S10 are high-traffic)

**A changed guide now conflicts with a project-specific supplement:**
- The supplement takes precedence for this project
- But also consider whether the master update is correct and your supplement should be updated
- If the master update is better, update your supplement to align with it

**You're unsure whether something is a conflict:**
- Default to updating your supplement to stay consistent with master
- Only deviate if there is a specific, documented reason this project needs a different approach

**Import script fails with "Master directory not found":**
`.shamt/shamt_master_path.conf` is stale — the master repo has moved or you're on a different machine. Update the file with the current path to your local `shamt-ai-dev` clone:

```bash
echo "/path/to/shamt-ai-dev" > .shamt/shamt_master_path.conf
```

**SHAMT-2 migration — `.shamt/last_sync.conf` not in your `.gitignore`:**
If your project was initialized before SHAMT-2, `.shamt/last_sync.conf` was not automatically gitignored at init time. The import script now writes this file after every run. Add it manually to your `.gitignore`:

```
.shamt/last_sync.conf
```

**SHAMT-3 migration — `.shamt/import_diff*.md` not in your `.gitignore`:**
If your project was initialized before SHAMT-3, import diff files were not automatically gitignored at init time. The import script writes these files on every run. Add them manually to your `.gitignore`:

```
.shamt/import_diff*.md
```

**SHAMT-4 migration — individual `.conf` entries not consolidated:**
If your project was initialized before SHAMT-4, your `.gitignore` may list `.shamt/shamt_master_path.conf` and `.shamt/last_sync.conf` individually, and `.shamt/rules_file_path.conf` may not be gitignored at all. Replace the individual entries with the wildcard:

```
.shamt/*.conf
```
